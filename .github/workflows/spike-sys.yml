name: spike-sys

on:
  pull_request:
    branches: [ main ]
    paths: "spike-sys/**"
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: |
        cd spike-sys
        bash build.sh
        cargo build
